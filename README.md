# Stock_Data_Cleaner
An automated AI-based data cleaning system that analyzes raw datasets and automatically fixes missing values, duplicates, incorrect data types, inconsistent text, and outliers. The project uses Python, machine learning, and statistical techniques to transform messy data into clean, structured, and analysis-ready datasets.
